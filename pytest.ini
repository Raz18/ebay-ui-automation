[pytest]
addopts = -v --tb=short
testpaths = tests
markers =
    smoke: Quick sanity tests for core functionality
    regression: Full regression suite across all features
    parallel: Tests verified safe for parallel execution
    e2e: End-to-end tests covering complete user flows
    browser_matrix: Parametrize test across all browser profiles
